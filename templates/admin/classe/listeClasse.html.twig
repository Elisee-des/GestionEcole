{% extends "base.admin.html.twig" %}

{% block title %}
	Liste de classe de l'annee {{ annee.nom }}
{% endblock %}

{% block titre %}
	Liste de classe de l'annee {{ annee.nom }}
{% endblock %}
{% block boutons %}
	<a href="{{ path("admin_classe_creer") }}" class="btn btn-inverse btn-outline-inverse btn-actions float-right">Ajouter un classe</a>
{% endblock %}

{% block body %}
	<div class="card">
		<div class="card-body">
			<div class="content">
				<table id="example" class="table table-striped nowrap" style="width:100%">
					<thead>
						<tr>
							<th>Classe</th>
							<th>Actions</th>
						</tr>
					</thead>
					<tbody>
						{% for classe in classes %}
							<tr>
								<td>{{classe.nom}}</td>
								<td>
									{# <a
										href="{{ path("admin_classe_editer", {"id": classe.id}) }}" class="btn btn-inverse btn-actions btn-outline-inverse">
										Editer
									</a> #}
									<a
										href="{{ path("admin_classe_editer", {"id": annee.id}) }}" title="Voir les classes de cette anneé" class="btn btn-info btn-outline-info" >
										<i class="ti-receipt"></i>
									</a>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
	</div>
{% endblock %}

{% block javascripts %}{% endblock %}
