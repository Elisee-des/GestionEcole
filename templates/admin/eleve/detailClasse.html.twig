{% extends "base.admin.html.twig" %}

{% block title %}
	Les Eleves de la classe de {{ classe.nom }}
{% endblock %}

{% block titre %}
	Les Eleves de la classe de {{ classe.nom }}
{% endblock %}
{% block boutons %}
	{# <a href="{{ path("admin_trimestre_creer") }}" class="btn btn-inverse btn-outline-inverse btn-actions float-right">Ajouter un trimestre</a> #}
{% endblock %}

{% block body %}
	<div class="card">
		<div class="card-body">
			<div class="content">
				<table id="example" class="table table-striped nowrap" style="width:100%">
					<thead>
						<tr>
							<th>Nom</th>
							<th>Prenom</th>
							<th>Actions</th>
						</tr>
					</thead>
					<tbody>
						{% for  eleve in eleves %}
							<tr>
								<td>{{eleve.nom}}</td>
								<td>{{eleve.prenom}}</td>
								<td>
									{# <a
										href="{{ path("admin_eleve_editer", {"id": trimestre.id}) }}" class="btn btn-inverse btn-actions btn-outline-inverse">
										Editer
									</a>

									<a
										href="{{ path("admin_trimestre_detail", {"id": trimestre.id}) }}" class="btn btn-info btn-outline-info">
										Detail
									</a>

									<a
										href="{{ path("admin_trimestre_editer", {"id": trimestre.id}) }}" class="btn btn-inverse btn-actions btn-outline-inverse">
										Trimestre
									</a> #}
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
	</div>
{% endblock %}

{% block javascripts %}{% endblock %}
